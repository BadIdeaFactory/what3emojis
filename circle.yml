machine:
  node:
    version: 4.4.3

dependencies:
  post:
    - npm install -g gulp

test:
  override:
    - echo 'no tests :·['

# deployment:
#   production:
#     branch: master
#     owner: circleci
#     commands:
#       - gulp build
#       - sftp -b ./deploy $PRODUCTION_USERNAME@$PRODUCTION_HOSTNAME
